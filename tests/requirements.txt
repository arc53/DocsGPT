pytest>=8.0.0
pytest-asyncio>=0.23
pytest-cov>=4.1.0
coverage>=7.4.0
pytest-postgresql>=6.0.0
psycopg[binary]>=3.1
cryptography>=46.0.0
# Runner engine for the sandbox integration test: tests launch a local
# `jupyter kernelgateway` process (no Docker). Not an app runtime dependency.
# ipykernel provides the python3 kernel the gateway launches per session.
jupyter-kernel-gateway>=2.5.0
ipykernel>=6.0.0
websocket-client>=1.6.0
# Document renderers the artifact_generator runs inside the sandbox kernel.
# In production these are pre-baked into the sandbox image; locally the kernel
# uses the app .venv, so the integration test needs them installed. python-pptx
# and openpyxl already ship in application/requirements.txt; only the rest are
# test-only here (kept out of application/requirements.txt — sandbox-image libs).
python-docx>=1.1
reportlab>=4.0,<5
