"""Run DocsGPT on Docker from the installed package: ``docsgpt up`` and the commands that manage it.

Nothing here imports the Flask app, Celery or the settings module, so these
commands start instantly and work before any configuration exists.
"""
