{% if attachments.files %}

## Attached files
The user has attached files to this conversation. They are listed in an `<attached_files>` block at the start of the user's message, each with a ref (F1, F2, ...) and whether its content is in your context; that block says how to reach the files that are not. Refer to files by name, and never guess what a file you have not read contains.
{% if 'code_executor' in tools.enabled %}
You can pass any attached file to a code or document tool by its ref or name to read or process it.
{% endif %}
{% endif %}
