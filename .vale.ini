# Minimal Vale configuration for testing
MinAlertLevel = warning
Vocab = DocsGPT

# Set the styles path (where Vale looks for styles and vocabularies)
StylesPath = .github/styles

[*.{md,mdx}]
BasedOnStyles = Vale
